@model List<Instructor>
<div>
    <h2>Instructors :</h2>
    @foreach (var instructor in Model)
    {
        <p>@instructor.Name <a asp-action="Details" asp-controller="Instructor" asp-route-Id="@instructor.Id" class="btn btn-primary">Details</a></p>
    }
</div>
